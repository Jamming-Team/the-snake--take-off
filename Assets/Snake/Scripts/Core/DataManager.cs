using XTools;

namespace Snake {
    public class DataManager : DataManagerBase<GameDataSO> {
        // protected override void Awake() {
        //     base.Awake();
        //     ServiceLocator.Global.Register(typeof(DataManager),this);
        // }
        
    }
}